# Copy to .env and fill values
# IMPORTANT: never commit real API keys. If you previously committed a real key, rotate it in Groq.
GROQ_API_KEY=your_groq_api_key_here

# Groq model (LLaMA 3 70B-class). Groq model names can change; override as needed.
GROQ_MODEL=llama-3.3-70b-versatile

# Paths (relative to backend/)
DOCS_ROOT=docs
FAISS_ROOT=storage/faiss

# Embeddings
EMBED_MODEL=sentence-transformers/all-MiniLM-L6-v2

# Retrieval
TOP_K=5

# Chunking (approximate token counting)
CHUNK_MIN_TOKENS=500
CHUNK_MAX_TOKENS=800
CHUNK_OVERLAP_TOKENS=80
